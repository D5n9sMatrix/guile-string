;; string-thgir

;; name project: guile-string
;; name program: thgir
;; func program: string

;; simple: easy value
;; inital: present
;; news: notices
;; roku: stream channels
;; fifo: headers

;; start file
;; loading ...

(define (string-thgir car motor)
  (if (> car motor)
             (begin (display "car=" car)
                    (newline)))
  (or (if (< motor car)
          (begin (display "motor=" motor)
                 (newline)))))

;; portability open car start connection
(string-thgir 10 80)
;; running barn's country all humanity packages
(let ((frumble "froz"))
  (define banana (lambda () (apple 'peach)))
  (define apple (lambda (x) x))
  (banana))

;; position car
;; types car list
(let ()
  (define car-list-x 512)
  (define car-list-y 512)
  (+ car-list-x car-list-y))

;; present to peoples 5000 mill
(define (string-thgir-val-x list-x)
    (if (> list-x 0)
        (begin display "5000 mill"))
    (if (> list-x 0)
        (begin display "5000 mill"))
    (or (if (>= list-x 0)
            (begin display "5000 mill"))))

;; running gates
;; loading ...
(define (string-thgir-val-y list-y)
    (if (> list-y 0)
        (begin display "5000 mill"))
    (if (> list-y 0)
        (begin display "5000 mill"))
    (or (if (>= list-y 0)
            (begin display "5000 mill"))))

